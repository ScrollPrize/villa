# todos
- why does the augmentation sheet show distorted (thicker) center lines on distorted samples? Given that the distortions should happen by changing the sampled coords they cannot reasonably change the opencv line thickness
