# Task: Add Test Dataset Evaluation And Snapshot Cadence

Implement the first item from `planning/todo.md`:

- Add testing data to the config. For now this can be one copied fiber supplied
  through config.
- Testing should run every configured test-iteration interval.
- Snapshot storage should happen at the test-iteration interval.
- Store a best-model snapshot as well.
