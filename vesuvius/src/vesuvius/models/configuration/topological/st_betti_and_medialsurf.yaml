tr_config:
  patch_size: [128,128,128]

dataset_config:
  min_labeled_ratio: 0 
  min_bbox_percent: 0 
  normalization_scheme: "zscore"
  skip_patch_validation: true
  targets:
    surface:
      activation: "none"
      losses:
        - name: "MedialSurfaceRecall"
          weight: 1.0
        - name: "BettiMatchingLoss"
          weight: 0.25
          kwargs:
            filtration: 'superlevel'
            chunk_mode: "grid"
            chunk_size: [12,128,128]
          start_epoch: 25

