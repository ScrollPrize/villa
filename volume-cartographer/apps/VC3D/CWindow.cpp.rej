diff a/volume-cartographer/apps/VC3D/CWindow.cpp b/volume-cartographer/apps/VC3D/CWindow.cpp	(rejected hunks)
@@ -2637,8 +2637,7 @@ CWindow::CWindow(size_t cacheSizeGB, RenderBenchOptions benchOptions) :
                     const auto path = _lineAnnotationController->fiberFilePath(fiberId);
                     if (!path.empty()) {
                         _spiralWorkspace->noteTrackedFiberSaved(
-                            fiberId, generation,
-                            QString::fromStdString(path.string()));
+                            generation, QString::fromStdString(path.string()));
                     }
                 }
             });
