# Task: unify cache and Z-sensitivity status

Correct the cache status text so the shared GiB unit appears once after both
RAM and disk values, and merge the Z-scroll sensitivity into the same permanent
status-bar label. The cache text and Z-sensitivity must not occupy overlapping
widgets.
