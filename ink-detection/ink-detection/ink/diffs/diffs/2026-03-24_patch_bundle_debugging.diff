diff --git a/ink/experiments/erm_patch_bundle.py b/ink/experiments/erm_patch_bundle.py
index e378f1d..75dd5ef 100644
--- a/ink/experiments/erm_patch_bundle.py
+++ b/ink/experiments/erm_patch_bundle.py
@@ -3,7 +3,7 @@ from __future__ import annotations
 from dataclasses import replace
 
 from ink.experiments.erm import EXPERIMENT as _ERM_EXPERIMENT
-from ink.recipes.data.patch_bundle import PatchBundleDataRecipe
+from ink.recipes.data.patch_bundle import GeneratedPatchBundleDataRecipe
 
 _BUNDLE_ROOT = ".tmp/patch_bundles/erm"
 _SOURCE_DATA = _ERM_EXPERIMENT.data
@@ -12,8 +12,9 @@ _SOURCE_DATA = _ERM_EXPERIMENT.data
 EXPERIMENT = replace(
     _ERM_EXPERIMENT,
     name="erm_patch_bundle",
-    data=PatchBundleDataRecipe(
+    data=GeneratedPatchBundleDataRecipe(
         bundle_root=_BUNDLE_ROOT,
+        source=_SOURCE_DATA,
         train_batch_size=int(_SOURCE_DATA.train_batch_size),
         valid_batch_size=_SOURCE_DATA.valid_batch_size,
         num_workers=int(_SOURCE_DATA.num_workers),
diff --git a/ink/recipes/data/layout.py b/ink/recipes/data/layout.py
index 81a5461..5ffa1c1 100644
--- a/ink/recipes/data/layout.py
+++ b/ink/recipes/data/layout.py
@@ -160,6 +160,29 @@ class NestedZarrLayout:
         )
         return hashlib.sha1(repr(payload).encode("utf-8")).hexdigest()
 
+    def segment_source_metadata_fingerprint(
+        self,
+        segment_id: str,
+        *,
+        label_suffix: str = "",
+        mask_suffix: str = "",
+    ) -> str:
+        """Hash only zarr metadata for the canonical volume, label, and supervision-mask sources."""
+        paths = self.resolve_paths(
+            segment_id,
+            label_suffix=label_suffix,
+            mask_suffix=mask_suffix,
+        )
+        payload = (
+            ("segment_id", str(segment_id)),
+            ("label_suffix", str(label_suffix)),
+            ("mask_suffix", str(mask_suffix)),
+            ("volume", _zarr_metadata_signature(paths.volume_path)),
+            ("inklabels", _zarr_metadata_signature(paths.inklabels_path)),
+            ("supervision_mask", _zarr_metadata_signature(paths.supervision_mask_path)),
+        )
+        return hashlib.sha1(repr(payload).encode("utf-8")).hexdigest()
+
 
 def _hash_file_contents(path: Path) -> str:
     digest = hashlib.sha1()
diff --git a/ink/recipes/data/patch_bundle/recipe.py b/ink/recipes/data/patch_bundle/recipe.py
index 7e82319..d4622f1 100644
--- a/ink/recipes/data/patch_bundle/recipe.py
+++ b/ink/recipes/data/patch_bundle/recipe.py
@@ -9,19 +9,23 @@ from torch.utils.data import Dataset
 import zarr
 
 from ink.core.types import DataBundle
+from ink.recipes.data.layout import NestedZarrLayout
 from ink.recipes.data.normalization import ClipMaxDiv255Normalization
-from ink.recipes.data.patch_bundle.writer import load_patch_bundle_manifest
+from ink.recipes.data.patch_bundle.writer import PatchBundleWriter, load_patch_bundle_manifest
 from ink.recipes.data.samplers import GroupBalancedSampler, GroupStratifiedSampler, ShuffleSampler
 from ink.recipes.data.transforms import (
     apply_eval_sample_transforms,
     apply_train_sample_transforms,
     build_joint_transform,
 )
+from ink.recipes.data.grouped_zarr_data import GroupedZarrPatchDataRecipe
 from ink.recipes.data.zarr_data import (
+    ZarrDataContext,
     build_patch_data_bundle,
     collate_grouped_batch,
     collate_patch_batch,
     count_group_idxs,
+    ZarrPatchDataRecipe,
 )
 
 
@@ -51,8 +55,17 @@ class PatchBundleDataset(Dataset):
         self.augment = augment
         self.normalization = normalization
         self.patch_size = int(self.manifest["extraction"]["patch_size"])
+        self.tile_size = int(self.manifest["extraction"]["tile_size"])
+        self.stride = int(self.manifest["extraction"]["stride"])
         self.in_channels = int(self.manifest["extraction"]["in_channels"])
+        self.dataset_root = str(self.manifest["dataset_root"])
+        self.label_suffix = str(self.manifest["extraction"].get("label_suffix") or "")
+        self.mask_suffix = str(self.manifest["extraction"].get("mask_suffix") or "")
         self.segment_ids = tuple(str(segment_id) for segment_id in self.manifest["segment_ids"])
+        self.layout = NestedZarrLayout(self.dataset_root)
+        self.segments = {segment_id: {} for segment_id in self.segment_ids}
+        self._volume_cache = {}
+        self._label_mask_store_cache = {}
 
         patches_root = self.bundle_root / self.split / "patches.zarr"
         if not patches_root.exists():
@@ -64,6 +77,10 @@ class PatchBundleDataset(Dataset):
         self._valid_mask = store["valid_mask"]
         self._xyxy = store["xyxy"]
         self._segment_index = store["segment_index"]
+        self._sample_segment_ids = tuple(
+            self.segment_ids[int(segment_idx)]
+            for segment_idx in np.asarray(self._segment_index[:], dtype=np.int32).tolist()
+        )
         self.transform = build_joint_transform(
             self.split,
             augment=self.augment,
@@ -72,9 +89,37 @@ class PatchBundleDataset(Dataset):
             in_channels=self.in_channels,
         )
 
+    def data_context(self) -> ZarrDataContext:
+        return ZarrDataContext(
+            layout=self.layout,
+            segments=self.segments,
+            in_channels=int(self.in_channels),
+            label_suffix=self.label_suffix,
+            mask_suffix=self.mask_suffix,
+            patch_index_cache_dir=None,
+            volume_cache=self._volume_cache,
+            label_mask_store_cache=self._label_mask_store_cache,
+        )
+
     def __len__(self) -> int:
         return int(self._x.shape[0])
 
+    def segment_sample_indices(self, segment_id: str) -> tuple[int, ...]:
+        segment_id = str(segment_id)
+        return tuple(
+            idx
+            for idx, sample_segment_id in enumerate(self._sample_segment_ids)
+            if sample_segment_id == segment_id
+        )
+
+    def build_view(self, *, split: str, sample_indices, segment_ids=()) -> "PatchBundleViewDataset":
+        return PatchBundleViewDataset(
+            base_dataset=self,
+            split=split,
+            sample_indices=sample_indices,
+            segment_ids=segment_ids,
+        )
+
     def _raw_item(self, idx: int):
         idx = int(idx)
         image = np.asarray(self._x[idx], dtype=np.uint8)
@@ -107,6 +152,63 @@ class PatchBundleDataset(Dataset):
         return image, label, valid_mask, xyxy, segment_id
 
 
+class PatchBundleViewDataset(Dataset):
+    def __init__(self, *, base_dataset: PatchBundleDataset, split: str, sample_indices, segment_ids=()):
+        self.base_dataset = base_dataset
+        self.split = str(split).strip().lower()
+        if self.split not in {"train", "valid"}:
+            raise ValueError(f"unknown split: {split!r}")
+        self.augment = base_dataset.augment
+        self.normalization = base_dataset.normalization
+        self.patch_size = int(base_dataset.patch_size)
+        self.in_channels = int(base_dataset.in_channels)
+        self.sample_indices = tuple(int(idx) for idx in sample_indices)
+        if segment_ids:
+            self.segment_ids = tuple(str(segment_id) for segment_id in segment_ids)
+        else:
+            seen = set()
+            ordered = []
+            for sample_idx in self.sample_indices:
+                segment_id = base_dataset._sample_segment_ids[int(sample_idx)]
+                if segment_id in seen:
+                    continue
+                seen.add(segment_id)
+                ordered.append(segment_id)
+            self.segment_ids = tuple(ordered)
+        self.transform = build_joint_transform(
+            self.split,
+            augment=self.augment,
+            normalization=self.normalization,
+            patch_size=self.patch_size,
+            in_channels=self.in_channels,
+        )
+
+    def __len__(self) -> int:
+        return len(self.sample_indices)
+
+    def __getitem__(self, idx):
+        source_idx = int(self.sample_indices[int(idx)])
+        image, label, valid_mask, xyxy, segment_id = self.base_dataset._raw_item(source_idx)
+        if self.split == "train":
+            image, label, valid_mask = apply_train_sample_transforms(
+                image,
+                label,
+                augment=self.augment,
+                patch_size=self.patch_size,
+                transform=self.transform,
+                valid_mask=valid_mask,
+            )
+        else:
+            image, label, valid_mask = apply_eval_sample_transforms(
+                image,
+                label,
+                patch_size=self.patch_size,
+                transform=self.transform,
+                valid_mask=valid_mask,
+            )
+        return image, label, valid_mask, xyxy, segment_id
+
+
 class GroupedPatchBundleDataset(PatchBundleDataset):
     def __init__(self, **kwargs):
         super().__init__(**kwargs)
@@ -251,9 +353,20 @@ class GroupedPatchBundleDataRecipe(PatchBundleDataRecipe):
         )
 
 
+@dataclass(frozen=True, kw_only=True)
+class GeneratedPatchBundleDataRecipe(PatchBundleDataRecipe):
+    source: ZarrPatchDataRecipe | GroupedZarrPatchDataRecipe
+
+    def build(self, *, runtime=None, augment=None) -> DataBundle:
+        PatchBundleWriter(self.source).ensure(out_root=Path(self.bundle_root).expanduser().resolve())
+        return super().build(runtime=runtime, augment=augment)
+
+
 __all__ = [
+    "GeneratedPatchBundleDataRecipe",
     "GroupedPatchBundleDataRecipe",
     "GroupedPatchBundleDataset",
     "PatchBundleDataRecipe",
     "PatchBundleDataset",
+    "PatchBundleViewDataset",
 ]
diff --git a/ink/recipes/data/patch_bundle/writer.py b/ink/recipes/data/patch_bundle/writer.py
index 19a5411..42fec71 100644
--- a/ink/recipes/data/patch_bundle/writer.py
+++ b/ink/recipes/data/patch_bundle/writer.py
@@ -5,12 +5,15 @@ import hashlib
 import json
 import logging
 from pathlib import Path
+import time
 from typing import Any
 
+import numpy as np
 import yaml
 import zarr
 
 from ink.recipes.data.grouped_zarr_data import GroupedZarrPatchDataRecipe, group_samples_by_split
+from ink.recipes.data.zarr_io import resolve_segment_volume
 from ink.recipes.data.zarr_data import (
     ZarrDataContext,
     ZarrPatchDataRecipe,
@@ -20,7 +23,8 @@ from ink.recipes.data.zarr_data import (
 )
 
 _PROGRESS_LOG = logging.getLogger("ink.progress")
-_PATCH_BUNDLE_SCHEMA_VERSION = 1
+_PATCH_BUNDLE_SCHEMA_VERSION = 3
+_BUNDLE_PROGRESS_EVERY = 250
 
 
 def _log(message: str) -> None:
@@ -91,7 +95,7 @@ def _source_fingerprint(*, layout, recipe, split_segment_ids: tuple[str, ...]) -
         "schema_version": int(_PATCH_BUNDLE_SCHEMA_VERSION),
         "dataset_root": str(recipe.dataset_root),
         "segments": {
-            str(segment_id): layout.segment_source_fingerprint(
+            str(segment_id): layout.segment_source_metadata_fingerprint(
                 str(segment_id),
                 label_suffix=str(recipe.label_suffix),
                 mask_suffix=str(recipe.mask_suffix),
@@ -120,6 +124,38 @@ def _write_manifest(path: Path, manifest: dict[str, Any]) -> None:
         yaml.safe_dump(manifest, handle, sort_keys=False)
 
 
+def _segment_specs_for_manifest(
+    *,
+    context: ZarrDataContext,
+    split_segment_ids: tuple[str, ...],
+) -> list[dict[str, Any]]:
+    specs: list[dict[str, Any]] = []
+    for segment_id in split_segment_ids:
+        volume = resolve_segment_volume(
+            layout=context.layout,
+            segments=context.segments,
+            segment_id=str(segment_id),
+            in_channels=int(context.in_channels),
+            volume_cache=context.volume_cache,
+        )
+        cache_key = (str(segment_id), context.label_suffix, context.mask_suffix)
+        label_mask_store = context.label_mask_store_cache.get(cache_key)
+        bbox_rows = None
+        if label_mask_store is not None and getattr(label_mask_store, "bbox_rows", None) is not None:
+            bbox_rows = [
+                [int(value) for value in row]
+                for row in tuple(getattr(label_mask_store, "bbox_rows") or ())
+            ]
+        specs.append(
+            {
+                "segment_id": str(segment_id),
+                "shape": [int(value) for value in volume.image_shape_hw],
+                "bbox": bbox_rows,
+            }
+        )
+    return specs
+
+
 def _create_array(root, name: str, *, shape: tuple[int, ...], dtype) -> Any:
     chunks = [1]
     chunks.extend(max(1, min(int(dim), 256)) for dim in shape[1:])
@@ -187,6 +223,7 @@ class PatchBundleWriter:
 
         written = {}
         for split_name, samples in samples_by_split.items():
+            split_started_at = time.perf_counter()
             split_dir = out_root / str(split_name)
             patches_root = _bundle_patches_root(out_root, split=split_name)
             manifest_path = _bundle_manifest_path(out_root, split=split_name)
@@ -223,18 +260,40 @@ class PatchBundleWriter:
                 segment_index_arr[idx] = int(segment_index_by_id[segment_id])
                 if group_idx_arr is not None:
                     group_idx_arr[idx] = int(sample[3])
+                sample_count = idx + 1
+                if (
+                    sample_count == len(samples)
+                    or sample_count == 1
+                    or sample_count % _BUNDLE_PROGRESS_EVERY == 0
+                ):
+                    elapsed_s = max(time.perf_counter() - split_started_at, 1e-9)
+                    samples_per_s = sample_count / elapsed_s
+                    _log(
+                        "[bundle] progress "
+                        f"split={split_name} "
+                        f"samples={sample_count}/{len(samples)} "
+                        f"samples_per_s={samples_per_s:.2f}"
+                    )
+
+            fingerprint_started_at = time.perf_counter()
+            source_fingerprint = _source_fingerprint(
+                layout=context.layout,
+                recipe=recipe,
+                split_segment_ids=split_segment_ids[split_name],
+            )
+            fingerprint_elapsed_s = time.perf_counter() - fingerprint_started_at
 
             manifest = {
                 "schema_version": int(_PATCH_BUNDLE_SCHEMA_VERSION),
                 "recipe_family": recipe_family,
                 "split": str(split_name),
-                "source_fingerprint": _source_fingerprint(
-                    layout=context.layout,
-                    recipe=recipe,
-                    split_segment_ids=split_segment_ids[split_name],
-                ),
+                "source_fingerprint": source_fingerprint,
                 "dataset_root": str(recipe.dataset_root),
                 "segment_ids": [str(segment_id) for segment_id in ordered_segment_ids],
+                "segment_specs": _segment_specs_for_manifest(
+                    context=context,
+                    split_segment_ids=ordered_segment_ids,
+                ),
                 "counts": {
                     "samples": int(len(samples)),
                     "segments": int(len(ordered_segment_ids)),
@@ -264,6 +323,25 @@ class PatchBundleWriter:
                     "dtype": str(group_idx_arr.dtype),
                 }
             _write_manifest(manifest_path, manifest)
+            split_elapsed_s = max(time.perf_counter() - split_started_at, 1e-9)
+            total_bytes = (
+                int(np.prod(x_arr.shape)) * np.dtype(x_arr.dtype).itemsize
+                + int(np.prod(y_arr.shape)) * np.dtype(y_arr.dtype).itemsize
+                + int(np.prod(valid_arr.shape)) * np.dtype(valid_arr.dtype).itemsize
+                + int(np.prod(xyxy_arr.shape)) * np.dtype(xyxy_arr.dtype).itemsize
+                + int(np.prod(segment_index_arr.shape)) * np.dtype(segment_index_arr.dtype).itemsize
+            )
+            if group_idx_arr is not None:
+                total_bytes += int(np.prod(group_idx_arr.shape)) * np.dtype(group_idx_arr.dtype).itemsize
+            mib_per_s = (total_bytes / (1024 * 1024)) / split_elapsed_s
+            _log(
+                "[bundle] wrote "
+                f"split={split_name} "
+                f"samples={len(samples)} "
+                f"elapsed_s={split_elapsed_s:.3f} "
+                f"fingerprint_s={fingerprint_elapsed_s:.3f} "
+                f"throughput_mib_s={mib_per_s:.2f}"
+            )
             written[split_name] = str(split_dir)
         return written
 
diff --git a/ink/recipes/stitch/loaders.py b/ink/recipes/stitch/loaders.py
index bdfb48e..cfb46f9 100644
--- a/ink/recipes/stitch/loaders.py
+++ b/ink/recipes/stitch/loaders.py
@@ -5,6 +5,7 @@ from collections.abc import Mapping
 import numpy as np
 from torch.utils.data import DataLoader
 
+from ink.recipes.data.patch_bundle import PatchBundleDataset
 from ink.recipes.data.patching import build_patch_index
 from ink.recipes.data.zarr_data import (
     ZarrPatchDataset,
@@ -22,7 +23,7 @@ from ink.recipes.stitch.infer_dataset import ZarrInferDataset
 
 def build_stitch_runtime_loaders(*, stitch_data: StitchData, train_loader, eval_loader) -> tuple[list[DataLoader], list[DataLoader]]:
     train_dataset = getattr(train_loader, "dataset", None)
-    if not isinstance(train_dataset, ZarrPatchDataset):
+    if not isinstance(train_dataset, (ZarrPatchDataset, PatchBundleDataset)):
         return [], []
 
     batch_size, num_workers = _resolve_loader_build_settings(train_loader=train_loader, eval_loader=eval_loader)
@@ -30,12 +31,20 @@ def build_stitch_runtime_loaders(*, stitch_data: StitchData, train_loader, eval_
     train_segment_ids = _segment_spec_ids(getattr(getattr(stitch_data, "train", None), "segments", ()))
     train_viz_loaders: list[DataLoader] = []
     if bool(getattr(train_viz, "enabled", False)) and train_segment_ids:
-        train_viz_loaders = build_zarr_segment_eval_loaders(
-            train_dataset,
-            segment_ids=train_segment_ids,
-            batch_size=batch_size,
-            num_workers=num_workers,
-        )
+        if isinstance(train_dataset, ZarrPatchDataset):
+            train_viz_loaders = build_zarr_segment_eval_loaders(
+                train_dataset,
+                segment_ids=train_segment_ids,
+                batch_size=batch_size,
+                num_workers=num_workers,
+            )
+        else:
+            train_viz_loaders = build_patch_bundle_segment_eval_loaders(
+                train_dataset,
+                segment_ids=train_segment_ids,
+                batch_size=batch_size,
+                num_workers=num_workers,
+            )
 
     log_only_segment_ids = tuple(
         str(segment_id)
@@ -43,15 +52,48 @@ def build_stitch_runtime_loaders(*, stitch_data: StitchData, train_loader, eval_
     )
     log_only_loaders: list[DataLoader] = []
     if log_only_segment_ids:
-        log_only_loaders = build_zarr_segment_infer_loaders(
-            train_dataset,
-            segment_ids=log_only_segment_ids,
-            batch_size=batch_size,
-            num_workers=num_workers,
-        )
+        if isinstance(train_dataset, ZarrPatchDataset):
+            log_only_loaders = build_zarr_segment_infer_loaders(
+                train_dataset,
+                segment_ids=log_only_segment_ids,
+                batch_size=batch_size,
+                num_workers=num_workers,
+            )
     return train_viz_loaders, log_only_loaders
 
 
+def build_patch_bundle_segment_eval_loaders(
+    dataset: PatchBundleDataset,
+    *,
+    segment_ids,
+    batch_size: int,
+    num_workers: int = 0,
+) -> list[DataLoader]:
+    if not isinstance(dataset, PatchBundleDataset):
+        return []
+    requested_segment_ids = tuple(str(segment_id) for segment_id in (segment_ids or ()))
+    loaders: list[DataLoader] = []
+    for segment_id in requested_segment_ids:
+        sample_indices = dataset.segment_sample_indices(segment_id)
+        segment_dataset = dataset.build_view(
+            split="valid",
+            sample_indices=sample_indices,
+            segment_ids=(segment_id,),
+        )
+        loaders.append(
+            DataLoader(
+                segment_dataset,
+                batch_size=max(1, int(batch_size)),
+                shuffle=False,
+                num_workers=max(0, int(num_workers)),
+                pin_memory=True,
+                drop_last=False,
+                collate_fn=collate_patch_batch,
+            )
+        )
+    return loaders
+
+
 def build_zarr_segment_eval_loaders(
     dataset,
     *,
diff --git a/ink/recipes/stitch/plan_from_zarr.py b/ink/recipes/stitch/plan_from_zarr.py
index 5537da3..1181292 100644
--- a/ink/recipes/stitch/plan_from_zarr.py
+++ b/ink/recipes/stitch/plan_from_zarr.py
@@ -1,15 +1,24 @@
 from __future__ import annotations
 
 from collections.abc import Mapping
+import logging
+import time
 
 import numpy as np
 
 from ink.core.types import DataBundle
 from ink.recipes.components import component_bboxes
+from ink.recipes.data.patch_bundle import PatchBundleDataset
 from ink.recipes.data.zarr_data import ZarrPatchDataset
 from ink.recipes.data.zarr_io import read_optional_supervision_mask_for_shape, read_supervision_mask_for_shape, resolve_segment_volume
 from ink.recipes.stitch.config import StitchData, StitchSegmentSpec
 
+_PROGRESS_LOG = logging.getLogger("ink.progress")
+
+
+def _log(message: str) -> None:
+    _PROGRESS_LOG.info(str(message))
+
 
 def derive_stitch_data_from_bundle(
     bundle: DataBundle,
@@ -19,9 +28,17 @@ def derive_stitch_data_from_bundle(
 ) -> StitchData:
     train_dataset = getattr(getattr(bundle, "train_loader", None), "dataset", None)
     eval_dataset = getattr(getattr(bundle, "eval_loader", None), "dataset", None)
-    if not isinstance(train_dataset, ZarrPatchDataset) or not isinstance(eval_dataset, ZarrPatchDataset):
+    supported_dataset_types = (ZarrPatchDataset, PatchBundleDataset)
+    if not isinstance(train_dataset, supported_dataset_types) or not isinstance(eval_dataset, supported_dataset_types):
         return stitch_data
 
+    derive_started_at = time.perf_counter()
+    _log(
+        "[stitch] derive start "
+        f"train_dataset={type(train_dataset).__name__} "
+        f"eval_dataset={type(eval_dataset).__name__}"
+    )
+
     if not _cfg_has_key(authored_config, "use_roi"):
         stitch_data.layout.use_roi = True
 
@@ -30,13 +47,22 @@ def derive_stitch_data_from_bundle(
         if not train_segment_ids:
             train_segment_ids = _ordered_segment_ids(train_dataset)
         if train_segment_ids:
-            stitch_data.train.segments = _derive_segment_specs(
-                train_dataset,
-                segment_ids=train_segment_ids,
-                downsample=int(stitch_data.layout.downsample),
-                require_supervision_mask=bool(stitch_data.layout.use_roi),
-                fallback_to_full_segment=not bool(stitch_data.layout.use_roi),
-                use_roi=bool(stitch_data.layout.use_roi),
+            started_at = time.perf_counter()
+            stitch_data.train.segments = _manifest_segment_specs(train_dataset, segment_ids=train_segment_ids)
+            if not stitch_data.train.segments:
+                stitch_data.train.segments = _derive_segment_specs(
+                    train_dataset,
+                    split_name="train",
+                    segment_ids=train_segment_ids,
+                    downsample=int(stitch_data.layout.downsample),
+                    require_supervision_mask=bool(stitch_data.layout.use_roi),
+                    fallback_to_full_segment=not bool(stitch_data.layout.use_roi),
+                    use_roi=bool(stitch_data.layout.use_roi),
+                )
+            _log(
+                "[stitch] derive train "
+                f"segments={len(stitch_data.train.segments)} "
+                f"dt_s={time.perf_counter() - started_at:.3f}"
             )
 
     if not _cfg_has_segment_specs(authored_config, "eval"):
@@ -44,32 +70,77 @@ def derive_stitch_data_from_bundle(
         if not eval_segment_ids:
             eval_segment_ids = _ordered_segment_ids(eval_dataset)
         if eval_segment_ids:
-            stitch_data.eval.segments = _derive_segment_specs(
-                eval_dataset,
-                segment_ids=eval_segment_ids,
-                downsample=int(stitch_data.layout.downsample),
-                require_supervision_mask=bool(stitch_data.layout.use_roi),
-                fallback_to_full_segment=not bool(stitch_data.layout.use_roi),
-                use_roi=bool(stitch_data.layout.use_roi),
+            started_at = time.perf_counter()
+            stitch_data.eval.segments = _manifest_segment_specs(eval_dataset, segment_ids=eval_segment_ids)
+            if not stitch_data.eval.segments:
+                stitch_data.eval.segments = _derive_segment_specs(
+                    eval_dataset,
+                    split_name="eval",
+                    segment_ids=eval_segment_ids,
+                    downsample=int(stitch_data.layout.downsample),
+                    require_supervision_mask=bool(stitch_data.layout.use_roi),
+                    fallback_to_full_segment=not bool(stitch_data.layout.use_roi),
+                    use_roi=bool(stitch_data.layout.use_roi),
+                )
+            _log(
+                "[stitch] derive eval "
+                f"segments={len(stitch_data.eval.segments)} "
+                f"dt_s={time.perf_counter() - started_at:.3f}"
             )
 
     if not _cfg_has_segment_specs(authored_config, "log_only") and stitch_data.log_only.segment_ids:
         log_only_segment_ids = tuple(str(segment_id) for segment_id in stitch_data.log_only.segment_ids)
+        started_at = time.perf_counter()
         stitch_data.log_only.segments = _derive_segment_specs(
             train_dataset,
+            split_name="log_only",
             segment_ids=log_only_segment_ids,
             downsample=int(stitch_data.layout.downsample),
             require_supervision_mask=False,
             fallback_to_full_segment=True,
             use_roi=bool(stitch_data.layout.use_roi),
         )
+        _log(
+            "[stitch] derive log_only "
+            f"segments={len(stitch_data.log_only.segments)} "
+            f"dt_s={time.perf_counter() - started_at:.3f}"
+        )
+
+    _log(f"[stitch] derive done dt_s={time.perf_counter() - derive_started_at:.3f}")
 
     return stitch_data
 
 
+def _manifest_segment_specs(dataset, *, segment_ids: tuple[str, ...]) -> list[StitchSegmentSpec]:
+    manifest = getattr(dataset, "manifest", None)
+    if not isinstance(manifest, Mapping):
+        return []
+    raw_specs = manifest.get("segment_specs")
+    if not isinstance(raw_specs, list):
+        return []
+    by_id = {
+        str(raw_spec.get("segment_id")): StitchSegmentSpec(
+            segment_id=str(raw_spec.get("segment_id")),
+            shape=tuple(raw_spec.get("shape") or ()),
+            bbox=None
+            if raw_spec.get("bbox") is None
+            else tuple(tuple(int(value) for value in row) for row in raw_spec.get("bbox") or ()),
+        )
+        for raw_spec in raw_specs
+        if isinstance(raw_spec, Mapping) and "segment_id" in raw_spec and "shape" in raw_spec
+    }
+    if not by_id:
+        return []
+    requested_ids = tuple(str(segment_id) for segment_id in segment_ids)
+    if any(segment_id not in by_id for segment_id in requested_ids):
+        return []
+    return [by_id[segment_id] for segment_id in requested_ids]
+
+
 def _derive_segment_specs(
-    dataset: ZarrPatchDataset,
+    dataset,
     *,
+    split_name: str,
     segment_ids,
     downsample: int,
     require_supervision_mask: bool,
@@ -79,6 +150,7 @@ def _derive_segment_specs(
     context = dataset.data_context()
     segment_specs = []
     for segment_id in segment_ids:
+        segment_started_at = time.perf_counter()
         volume = resolve_segment_volume(
             layout=context.layout,
             segments=context.segments,
@@ -103,6 +175,12 @@ def _derive_segment_specs(
                 bbox=bbox_rows,
             )
         )
+        _log(
+            "[stitch] derive segment "
+            f"split={split_name} "
+            f"segment={segment_id} "
+            f"dt_s={time.perf_counter() - segment_started_at:.3f}"
+        )
     return segment_specs
 
 
diff --git a/tests/test_data_recipe_behavior.py b/tests/test_data_recipe_behavior.py
index 53f3f28..662cf33 100644
--- a/tests/test_data_recipe_behavior.py
+++ b/tests/test_data_recipe_behavior.py
@@ -18,6 +18,7 @@ from ink.recipes.data.normalization import (
 )
 from ink.recipes.data.grouped_zarr_data import GroupedZarrPatchDataRecipe
 from ink.recipes.data.patch_bundle import (
+    GeneratedPatchBundleDataRecipe,
     GroupedPatchBundleDataRecipe,
     PatchBundleDataRecipe,
     PatchBundleWriter,
@@ -695,6 +696,58 @@ class ZarrPatchDataRecipeTests(unittest.TestCase):
             self.assertEqual(bundle_eval.meta.segment_ids, source_eval.meta.segment_ids)
             self.assertTrue(torch.equal(bundle_eval.meta.patch_xyxy, source_eval.meta.patch_xyxy))
 
+    def test_generated_patch_bundle_recipe_ensures_bundle_before_loading(self):
+        with tempfile.TemporaryDirectory() as tmpdir:
+            root = Path(tmpdir)
+            bundle_root = root / "bundle"
+            train_segment = _make_canonical_segment(
+                root,
+                group_name="group_a",
+                segment_id="segA",
+                volume=np.stack([np.full((8, 8), value, dtype=np.uint8) for value in (10, 20, 30, 40)], axis=0),
+                label=np.full((8, 8), 255, dtype=np.uint8),
+                supervision_mask=np.full((8, 8), 255, dtype=np.uint8),
+            )
+            val_segment = _make_canonical_segment(
+                root,
+                group_name="group_a",
+                segment_id="segB",
+                volume=np.stack([np.full((8, 8), value, dtype=np.uint8) for value in (50, 60, 70, 80)], axis=0),
+                label=np.full((8, 8), 0, dtype=np.uint8),
+                supervision_mask=np.full((8, 8), 255, dtype=np.uint8),
+            )
+
+            source_recipe = ZarrPatchDataRecipe(
+                dataset_root=str(root),
+                segments={"segA": train_segment, "segB": val_segment},
+                train_segment_ids=("segA",),
+                val_segment_ids=("segB",),
+                in_channels=4,
+                patch_size=8,
+                train_batch_size=1,
+                valid_batch_size=1,
+                shuffle=False,
+                normalization=ClipMaxDiv255Normalization(),
+            )
+
+            with (
+                mock.patch("ink.recipes.data.patch_bundle.recipe.PatchBundleWriter") as writer_cls,
+                mock.patch.object(PatchBundleDataRecipe, "build", return_value="bundle") as build_mock,
+            ):
+                out = GeneratedPatchBundleDataRecipe(
+                    bundle_root=str(bundle_root),
+                    source=source_recipe,
+                    train_batch_size=1,
+                    valid_batch_size=1,
+                    shuffle=False,
+                    normalization=ClipMaxDiv255Normalization(),
+                ).build(augment=_neutral_augment())
+
+            writer_cls.assert_called_once_with(source_recipe)
+            writer_cls.return_value.ensure.assert_called_once_with(out_root=bundle_root.resolve())
+            build_mock.assert_called_once()
+            self.assertEqual(out, "bundle")
+
     def test_patch_bundle_writer_ensure_reuses_existing_bundle(self):
         with tempfile.TemporaryDirectory() as tmpdir:
             root = Path(tmpdir)
@@ -780,10 +833,8 @@ class ZarrPatchDataRecipeTests(unittest.TestCase):
             writer = PatchBundleWriter(source_recipe)
             writer.ensure(out_root=bundle_root)
 
-            _write_zarr_array(
-                root / "group_a" / "segA" / "segA_inklabels.zarr",
-                np.full((8, 8), 0, dtype=np.uint8),
-            )
+            inklabels_attrs = root / "group_a" / "segA" / "segA_inklabels.zarr" / ".zattrs"
+            inklabels_attrs.write_text('{"bundle_test":"stale"}', encoding="utf-8")
 
             with mock.patch.object(PatchBundleWriter, "write", return_value={"train": "x", "valid": "y"}) as write_mock:
                 rewritten = writer.ensure(out_root=bundle_root)
diff --git a/tests/test_erm_experiment_behavior.py b/tests/test_erm_experiment_behavior.py
index 65a69cd..99c71f0 100644
--- a/tests/test_erm_experiment_behavior.py
+++ b/tests/test_erm_experiment_behavior.py
@@ -13,6 +13,10 @@ from torch.utils.data import DataLoader
 
 from ink.core import Batch, BatchMeta, assemble_experiment, build_experiment_data
 from ink.experiments import ERM_EXPERIMENT, ERM_PATCH_BUNDLE_EXPERIMENT, ERM_STITCH_INFERENCE_EXPERIMENT
+from ink.experiments.erm_debug_autogrown_w00 import EXPERIMENT as ERM_DEBUG_AUTOGROWN_W00_EXPERIMENT
+from ink.experiments.erm_patch_bundle_debug_autogrown_w00 import (
+    EXPERIMENT as ERM_PATCH_BUNDLE_DEBUG_AUTOGROWN_W00_EXPERIMENT,
+)
 from ink.experiments import run as experiment_run
 from ink.recipes.augment import TrainAugment
 from ink.recipes.data.normalization import ClipMaxDiv255Normalization
@@ -248,6 +252,7 @@ class ERMExperimentAssemblyTests(unittest.TestCase):
         self.assertEqual(ERM_PATCH_BUNDLE_EXPERIMENT.name, "erm_patch_bundle")
         self.assertIsInstance(ERM_PATCH_BUNDLE_EXPERIMENT.data, PatchBundleDataRecipe)
         self.assertEqual(ERM_PATCH_BUNDLE_EXPERIMENT.data.bundle_root, ".tmp/patch_bundles/erm")
+        self.assertIs(ERM_PATCH_BUNDLE_EXPERIMENT.data.source, ERM_EXPERIMENT.data)
         self.assertEqual(ERM_PATCH_BUNDLE_EXPERIMENT.data.train_batch_size, ERM_EXPERIMENT.data.train_batch_size)
         self.assertEqual(ERM_PATCH_BUNDLE_EXPERIMENT.data.valid_batch_size, ERM_EXPERIMENT.data.valid_batch_size)
         self.assertEqual(ERM_PATCH_BUNDLE_EXPERIMENT.data.num_workers, ERM_EXPERIMENT.data.num_workers)
@@ -258,6 +263,41 @@ class ERMExperimentAssemblyTests(unittest.TestCase):
         self.assertIs(ERM_PATCH_BUNDLE_EXPERIMENT.trainer, ERM_EXPERIMENT.trainer)
         self.assertIs(ERM_PATCH_BUNDLE_EXPERIMENT.evaluator, ERM_EXPERIMENT.evaluator)
 
+    def test_debug_patch_bundle_experiment_template_reuses_debug_erm_except_for_data(self):
+        self.assertEqual(
+            ERM_PATCH_BUNDLE_DEBUG_AUTOGROWN_W00_EXPERIMENT.name,
+            "erm_patch_bundle_debug_autogrown_w00",
+        )
+        self.assertIsInstance(ERM_PATCH_BUNDLE_DEBUG_AUTOGROWN_W00_EXPERIMENT.data, PatchBundleDataRecipe)
+        self.assertEqual(
+            ERM_PATCH_BUNDLE_DEBUG_AUTOGROWN_W00_EXPERIMENT.data.bundle_root,
+            ".tmp/patch_bundles/erm_debug_autogrown_w00",
+        )
+        self.assertIs(
+            ERM_PATCH_BUNDLE_DEBUG_AUTOGROWN_W00_EXPERIMENT.data.source,
+            ERM_DEBUG_AUTOGROWN_W00_EXPERIMENT.data,
+        )
+        self.assertIs(
+            ERM_PATCH_BUNDLE_DEBUG_AUTOGROWN_W00_EXPERIMENT.model,
+            ERM_DEBUG_AUTOGROWN_W00_EXPERIMENT.model,
+        )
+        self.assertIs(
+            ERM_PATCH_BUNDLE_DEBUG_AUTOGROWN_W00_EXPERIMENT.runtime,
+            ERM_DEBUG_AUTOGROWN_W00_EXPERIMENT.runtime,
+        )
+        self.assertIs(
+            ERM_PATCH_BUNDLE_DEBUG_AUTOGROWN_W00_EXPERIMENT.augment,
+            ERM_DEBUG_AUTOGROWN_W00_EXPERIMENT.augment,
+        )
+        self.assertIs(
+            ERM_PATCH_BUNDLE_DEBUG_AUTOGROWN_W00_EXPERIMENT.trainer,
+            ERM_DEBUG_AUTOGROWN_W00_EXPERIMENT.trainer,
+        )
+        self.assertIs(
+            ERM_PATCH_BUNDLE_DEBUG_AUTOGROWN_W00_EXPERIMENT.evaluator,
+            ERM_DEBUG_AUTOGROWN_W00_EXPERIMENT.evaluator,
+        )
+
     def test_erm_experiment_can_be_replaced_with_real_model_recipe(self):
         train, val = _patch_samples(train_count=4, val_count=2)
         experiment = replace(
diff --git a/tests/test_stitch_recipe_behavior.py b/tests/test_stitch_recipe_behavior.py
index 03b2089..28e7e3a 100644
--- a/tests/test_stitch_recipe_behavior.py
+++ b/tests/test_stitch_recipe_behavior.py
@@ -17,6 +17,7 @@ import zarr
 from ink.core import Batch, BatchMeta, DataBundle
 from ink.recipes.augment import TrainAugment
 from ink.recipes.data.normalization import ClipMaxDiv255Normalization
+from ink.recipes.data.patch_bundle import PatchBundleDataRecipe, PatchBundleWriter
 from ink.recipes.data.zarr_data import ZarrPatchDataRecipe
 from ink.recipes import stitch as stitch_pkg
 from ink.recipes.stitch import loaders as stitch_loaders
@@ -316,6 +317,116 @@ class StitchRuntimeDataTests(unittest.TestCase):
             self.assertEqual([spec.segment_id for spec in runtime.data.eval.segments], ["segB"])
             self.assertEqual(runtime.data.eval.segments[0].bbox, ((0, 8, 0, 8),))
 
+    def test_stitch_runtime_recipe_derives_train_and_eval_segments_from_patch_bundle(self):
+        with tempfile.TemporaryDirectory() as tmpdir:
+            root = Path(tmpdir)
+            bundle_root = root / "bundle"
+            seg_a = _make_canonical_segment(
+                root,
+                group_name="group_a",
+                segment_id="segA",
+                volume=np.stack([np.full((8, 8), value, dtype=np.uint8) for value in (10, 20, 30, 40)], axis=0),
+                label=np.full((8, 8), 255, dtype=np.uint8),
+                supervision_mask=np.full((8, 8), 255, dtype=np.uint8),
+            )
+            seg_b = _make_canonical_segment(
+                root,
+                group_name="group_a",
+                segment_id="segB",
+                volume=np.stack([np.full((8, 8), value, dtype=np.uint8) for value in (50, 60, 70, 80)], axis=0),
+                label=np.full((8, 8), 255, dtype=np.uint8),
+                supervision_mask=np.full((8, 8), 255, dtype=np.uint8),
+            )
+            source_recipe = ZarrPatchDataRecipe(
+                dataset_root=str(root),
+                segments={"segA": seg_a, "segB": seg_b},
+                train_segment_ids=("segA",),
+                val_segment_ids=("segB",),
+                in_channels=4,
+                patch_size=8,
+                train_batch_size=1,
+                valid_batch_size=1,
+                shuffle=False,
+                normalization=ClipMaxDiv255Normalization(),
+            )
+            PatchBundleWriter(source_recipe).write(out_root=bundle_root)
+            bundle = PatchBundleDataRecipe(
+                bundle_root=str(bundle_root),
+                train_batch_size=1,
+                valid_batch_size=1,
+                shuffle=False,
+                normalization=ClipMaxDiv255Normalization(),
+            ).build(augment=_neutral_augment())
+
+            runtime = StitchRuntimeRecipe().build(bundle)
+
+            self.assertTrue(runtime.data.layout.use_roi)
+            self.assertEqual([spec.segment_id for spec in runtime.data.train.segments], ["segA"])
+            self.assertEqual(runtime.data.train.segments[0].bbox, ((0, 8, 0, 8),))
+            self.assertEqual([spec.segment_id for spec in runtime.data.eval.segments], ["segB"])
+            self.assertEqual(runtime.data.eval.segments[0].bbox, ((0, 8, 0, 8),))
+
+    def test_build_stitch_runtime_loaders_supports_patch_bundle_train_viz(self):
+        with tempfile.TemporaryDirectory() as tmpdir:
+            root = Path(tmpdir)
+            bundle_root = root / "bundle"
+            seg_a = _make_canonical_segment(
+                root,
+                group_name="group_a",
+                segment_id="segA",
+                volume=np.stack([np.full((8, 8), value, dtype=np.uint8) for value in (10, 20, 30, 40)], axis=0),
+                label=np.full((8, 8), 255, dtype=np.uint8),
+                supervision_mask=np.full((8, 8), 255, dtype=np.uint8),
+            )
+            seg_b = _make_canonical_segment(
+                root,
+                group_name="group_a",
+                segment_id="segB",
+                volume=np.stack([np.full((8, 8), value, dtype=np.uint8) for value in (50, 60, 70, 80)], axis=0),
+                label=np.full((8, 8), 255, dtype=np.uint8),
+                supervision_mask=np.full((8, 8), 255, dtype=np.uint8),
+            )
+            source_recipe = ZarrPatchDataRecipe(
+                dataset_root=str(root),
+                segments={"segA": seg_a, "segB": seg_b},
+                train_segment_ids=("segA",),
+                val_segment_ids=("segB",),
+                in_channels=4,
+                patch_size=8,
+                train_batch_size=1,
+                valid_batch_size=1,
+                shuffle=False,
+                normalization=ClipMaxDiv255Normalization(),
+            )
+            PatchBundleWriter(source_recipe).write(out_root=bundle_root)
+            bundle = PatchBundleDataRecipe(
+                bundle_root=str(bundle_root),
+                train_batch_size=1,
+                valid_batch_size=1,
+                shuffle=False,
+                normalization=ClipMaxDiv255Normalization(),
+            ).build(augment=_neutral_augment())
+            runtime = StitchRuntimeRecipe(
+                {
+                    "train": {
+                        "viz": {
+                            "enabled": True,
+                        },
+                    },
+                }
+            ).build(bundle)
+
+            train_viz_loaders, log_only_loaders = stitch_loaders.build_stitch_runtime_loaders(
+                stitch_data=runtime.data,
+                train_loader=bundle.train_loader,
+                eval_loader=bundle.eval_loader,
+            )
+
+            self.assertEqual(len(train_viz_loaders), 1)
+            self.assertEqual(log_only_loaders, [])
+            first_batch = next(iter(train_viz_loaders[0]))
+            self.assertEqual(set(first_batch.meta.segment_ids), {"segA"})
+
     def test_stitch_runtime_recipe_uses_explicit_train_and_eval_segment_id_overrides(self):
         with tempfile.TemporaryDirectory() as tmpdir:
             root = Path(tmpdir)
